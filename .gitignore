# Build artifacts
/target

# Cargo lock (not needed for binaries/benchmarks)
Cargo.lock

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Criterion benchmark data (regenerated on each run)
# Uncomment if you don't want to track historical benchmark data
# /target/criterion/

# Legacy dataset download cache (the downloader has been removed)
.dataset_cache/

# Extracted SQL corpus. Committed compressed as datasets.tar.zst (tracked) and
# auto-extracted on first use. The extracted tree is ignored.
datasets/
