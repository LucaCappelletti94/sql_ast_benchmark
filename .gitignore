# Build artifacts
/target

# Cargo lock (not needed for binaries/benchmarks)
Cargo.lock

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Criterion benchmark data (regenerated on each run)
# Uncomment if you don't want to track historical benchmark data
# /target/criterion/

# Legacy dataset download cache (the downloader has been removed)
.dataset_cache/

# Extracted SQL corpus. The corpus is committed compressed as datasets.tar.zst;
# extract it with `tar --zstd -xf datasets.tar.zst`. The extracted tree is
# ignored (datasets.tar.zst itself is tracked).
datasets/
